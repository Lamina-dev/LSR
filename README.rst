LSR 目录
================================================================================

本文档库使用 reStructuredText 与 Sphinx。

构建命令：

.. code-block:: sh

   python -m pip install -r requirements.txt
   python -m sphinx -b html . _build/html

当前规范入口见 ``index.rst``。
